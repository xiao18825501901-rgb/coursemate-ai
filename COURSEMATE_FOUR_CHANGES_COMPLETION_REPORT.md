# CourseMate 四项产品改造完成报告

日期：2026-09-20  
分支：`fix/codex-dsh-audit-20260919`  
起始 HEAD：`89b04e8ff8a507a4fcdf5f81e8fe1bed889ba86e`

冻结应用 SHA：`e6c77dc9ebe779efea2882b92e1f1e99425d4cc8`

## 结论

四项改造已在当前真实源码中完成，并通过本地合成后端、迁移、单元与真实浏览器
集成验证。没有调用真实千问、没有迁移生产库、没有部署网站；线上状态不能由本地
结果推断。

## 1. 跨栏提问退役

题目 Step 后的粉色问题条、点击区域、左栏自动注入、自动节点切换、Bridge 横幅和
跨栏返回模式已从实际渲染/调用链删除。旧 Bridge 数据与历史读取保留；旧写动作明确
返回 410。题目“详解”保留为独立可拖动、缩放、关闭浮窗，并验证不改变左侧对话。

## 2. 三级推理强度

左右 Pane 独立支持中/高/最高，交互覆盖鼠标、键盘和触屏，偏好立即持久化且只影响
下一次提交。预算为中=B、高=2B、最高=null；Thinking 与预算档位正交。真实生产 B
未核验，系统在 Qwen 模式缺值时安全失败，不使用 USD 1.50 或任意默认值代替。

## 3. 注册自动学生资格

活动注册身份在当前请求内取得持久化 `registered` 资格。已有认证来源不覆盖，禁用
身份不复活，管理员权限不扩张，旧码和审计记录保留。首次创建 Pair、加入校园课程、
读取内容及开始学习有集成测试覆盖。

## 4. 全站主题

控制面板标题行新增太阳/月亮切换；默认浅色，用户偏好服务端持久化。深色覆盖完整
外壳、课程、文件、评论、日历、收件箱、帮助、知识树、双 Pane、输入、历史、测评、
详解和 portal/fullscreen。原始 PDF/图片不变色。

## 测试与证据

最新本地证据见 `docs/four-changes/RELEASE_EVIDENCE.md`。浏览器回归为 19/19 与
14/14；Web 60/60、Agent 66/66、针对性后端 97/97；完整 RAG 后端冻结回归为
698/698。以上均对应冻结应用 SHA，不沿用旧版本数字。

## 数据与回滚

UI Schema 升至 13，增量增加布局强度、run 预算快照及主题偏好。迁移不删除课程、
文件、历史、Bridge、资格、知识树或学习证据。生产回滚必须使用匹配应用构建与迁移前
数据库/上传目录备份，不能让旧代码直接打开新 Schema。

## 未完成的外部层次

- 真实 Qwen 费用与功能：NOT VERIFIED。
- 真实 Clerk 新注册/禁用账户：NOT VERIFIED。
- 生产备份、迁移、发布与多用户验收：NOT EXECUTED。

在没有新发布授权时，本轮停在本地 release candidate，不复用历史 canary 或部署授权。
